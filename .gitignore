node_modules/
# Build target of the card — the integration serves it from there, and the
# release workflow puts it into the zip.
custom_components/crowdsec/www/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
.coverage
htmlcov/
.DS_Store

# Contains the target host of a private instance — builddeploy.sh itself is
# tracked and reads it.
.env

# Local build counter of the card, per working copy.
card/.build-number
.venv-ha/
